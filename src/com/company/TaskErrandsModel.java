package com.company;

import java.util.Date;

// Subclass used to create Errands-tasks.

class TaskErrandsModel extends TaskModel {
    TaskErrandsModel(Date date, String type, String task ){
        super(date, type , task );
    }
}
